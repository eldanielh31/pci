# compilador de C/C++ y las herramientas de desarrollo del kernel
sudo apt update
sudo apt install build-essential linux-headers-$(uname -r)
# descarga el código fuente del kernel de Linux
sudo apt install linux-source

